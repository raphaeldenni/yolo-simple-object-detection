[project]
name = "yolo-simple-object-detection"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lapx>=0.5.11",
    "opencv-python>=4.10.0.84",
    "ultralytics>=8.3.29",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
